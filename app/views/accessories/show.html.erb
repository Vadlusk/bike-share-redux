<%= @accessory.title %>
<%= @accessory.description %>
<%= @accessory.price %>
<img src="<%= @accessory.image_url %>" alt="" class="accessory_image">
<% if @active %>
  <%= button_to 'Add to Cart', carts_path %>
<% else %>
  <p>Accessory Retired</p>
<% end %>
